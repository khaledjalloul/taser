services:
  taser:
    image: khaledjalloul/taser
    build:
      context: ..
      dockerfile: docker/Dockerfile
      args:
        USERNAME: ${USERNAME} # Set as empty string to use root
        ISAACLAB_VERSION: 2.1.0 # 2.1.0 / 2.2.0
      network: host # Temporary fix for when using VPN
    container_name: taser
    hostname: taser
    network_mode: host # Temporary fix for when using VPN
    environment:
      - DISPLAY=${DISPLAY}
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=all
      - OMNI_KIT_ALLOW_ROOT=1 # In case the user was root
    runtime: nvidia
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [ gpu ]
    entrypoint: bash docker/entrypoint.bash
    command: bash -c "while sleep 1000; do :; done"
    volumes:
      # Bind source code
      - type: bind
        source: ..
        target: /workspace/taser
      # Enable GUI
      - type: bind
        source: /tmp/.X11-unix
        target: /tmp/.X11-unix
      # Xauthority for X11 forwarding
      - type: bind
        source: ${HOME}/.Xauthority
        target: ${HOME}/.Xauthority # Change to /root if running as root
        read_only: false
      # Isaac Sim kit cache
      - type: bind
        source: ${HOME}/docker/taser/cache/kit
        target: /isaac-sim/kit/cache
      # OV cache
      - type: bind
        source: ${HOME}/docker/taser/cache/ov
        target: ${HOME}/.cache/ov
      # pip cache
      - type: bind
        source: ${HOME}/docker/taser/cache/pip
        target: ${HOME}/.cache/pip
      # NVIDIA GLCache
      - type: bind
        source: ${HOME}/docker/taser/cache/glcache
        target: ${HOME}/.cache/nvidia/GLCache
      # NVIDIA ComputeCache
      - type: bind
        source: ${HOME}/docker/taser/cache/computecache
        target: ${HOME}/.nv/ComputeCache
      # Omniverse logs
      - type: bind
        source: ${HOME}/docker/taser/logs
        target: ${HOME}/.nvidia-omniverse/logs
      # Omniverse data
      - type: bind
        source: ${HOME}/docker/taser/data
        target: ${HOME}/.local/share/ov/data
      # Documents
      - type: bind
        source: ${HOME}/docker/taser/documents
        target: ${HOME}/Documents
